[
  {
    "type": "function",
    "function": {
      "name": "getItemsInPath",
      "description": "Returns all files and folders inside a given directory path.",
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "Path to get the items from"
          }
        },
        "required": ["path"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "createFile",
      "description": "creates new empty file or overrides pre-existing one into an empty one",
      "parameters": {
        "type": "object",
        "properties": {
          "file": {
            "type": "string",
            "description": "file to be created"
          }
        },
        "required": ["file"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "readFile",
      "description": "Reads and returns the full contents of a file.",
      "parameters": {
        "type": "object",
        "properties": {
          "file": {
            "type": "string",
            "description": "File path to read."
          }
        },
        "required": ["file"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "readPdfPages",
      "description": "Reads and returns the text content of specific pages from a PDF file. Page numbers are 1-indexed (page 1 is the first page). Use this for PDFs instead of other read functions. Try to read as few pages at a time to avoid overwhelming the LLM.",
      "parameters": {
        "type": "object",
        "properties": {
          "file": {
            "type": "string",
            "description": "PDF file path to read."
          },
          "start_page": {
            "type": "integer",
            "description": "The page number to start reading from (1-indexed, inclusive)."
          },
          "end_page": {
            "type": "integer",
            "description": "The page number to stop reading at (1-indexed, inclusive)."
          }
        },
        "required": ["file", "start_page", "end_page"]
  }
    }
  }
  ,
  {
    "type": "function",
    "function": {
      "name": "readFileLines",
      "description": "Reads specific lines from a file. Use this instead of readFile for large files or when you only need a specific section. Line numbers are 1-indexed (line 1 is the first line).",
      "parameters": {
        "type": "object",
        "properties": {
          "file": {
            "type": "string",
            "description": "File path to read."
          },
          "start_line": {
            "type": "integer",
            "description": "The line number to start reading from (1-indexed, inclusive)."
          },
          "end_line": {
            "type": "integer",
            "description": "The line number to stop reading at (1-indexed, inclusive)."
          }
        },
        "required": ["file", "start_line", "end_line"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "delete",
      "description": "Deletes a file at the specified path.",
      "parameters": {
        "type": "object",
        "properties": {
          "file": {
            "type": "string",
            "description": "File path to delete."
          }
        },
        "required": ["file"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "createDirectory",
      "description": "Creates a new directory at the specified path.",
      "parameters": {
        "type": "object",
        "properties": {
          "directory": {
            "type": "string",
            "description": "Directory path to create."
          }
        },
        "required": ["directory"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "deleteDirectory",
      "description": "Deletes a directory at the specified path.",
      "parameters": {
        "type": "object",
        "properties": {
          "directory": {
            "type": "string",
            "description": "Directory path to delete."
          }
        },
        "required": ["directory"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "getCurrentDirectory",
      "description": "Returns the current working directory path.",
      "parameters": {
        "type": "object",
        "properties": {}
      }
    }
  },
  {
    "type":"function",
    "function":{
      "name": "fileExists",
      "description": "Checks if a file exists at the specified path.",
      "parameters": {
        "type": "object",
        "properties": {
          "file": {
            "type": "string",
            "description": "File path to check."
          }
        },
        "required": ["file"]
      }
    }
  },
  {"type": "function",
    "function": {
      "name": "getFileSize",
      "description": "Returns the size of a file in bytes.",
      "parameters": {
        "type": "object",
        "properties": {
          "file": {
            "type": "string",
            "description": "File path to get the size of."
          }
        },
        "required": ["file"]
      }
    }},
    {
    "type": "function",
    "function": {
      "name": "renameFile",
      "description": "Renames a file from old name to new name. Must include full path in both file and new name.",
      "parameters": {
        "type": "object",
        "properties": {
          "source": {
            "type": "string",
            "description": "The full path of the source file."
          },
          "new_name": {
            "type": "string",
            "description": "The new full path of the file."
          }
        },
        "required": ["source", "new_name"]
      }
    }},
    {
    "type": "function",
    "function": {
      "name": "rememberFact",
      "description": "Remembers a fact by storing it in memory.",
      "parameters": {
        "type": "object",
        "properties": {
          "key": {
            "type": "string",
            "description": "The key associated with the fact."
          },
          "fact": {
            "type": "string",
            "description": "The fact to be remembered."
          }
        },
        "required": ["key", "fact"]
      }
    }},
    {"type": "function",
    "function": {
      "name": "recallFact",
      "description": "Recalls a fact from memory using its associated key.",
      "parameters": {
        "type": "object",
        "properties": {
          "key": {
            "type": "string",
            "description": "The key associated with the fact to be recalled."
          }
        },
        "required": ["key"]
      }
    }},
  {
    "type": "function",
    "function": {
      "name": "forgetFact",
      "description": "Forgets a fact from memory using its associated key.",
      "parameters": {
        "type": "object",
        "properties": {
          "key": {
            "type": "string",
            "description": "The key associated with the fact to be forgotten."
          }
        },
        "required": ["key"]
      }
    }
  },
  {"type": "function",
    "function": {
      "name": "listMemories",
      "description": "Lists all keys of the facts currently stored in memory.",
      "parameters": {
        "type": "object",
        "properties": {}
      }
    }
  }
  ,
  {
    "type": "function",
    "function": {
      "name": "moveMultipleFiles",
      "description": "Moves multiple files from source paths to a destination directory. Can also be used for a single file by providing a list with one item. Use this to move files and folders around the filesystem.",
      "parameters": {
        "type": "object",
        "properties": {
          "sources": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "List of source file paths to move."
          },
          "destination": {
            "type": "string",
            "description": "Destination directory path to move the files to."
          }
        },
        "required": ["sources", "destination"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "copyMultipleFiles",
      "description": "Copies multiple files from source paths to a destination directory. Can also be used for a single file by providing a list with one item. Use this to copy files and folders around the filesystem.",
      "parameters": {
        "type": "object",
        "properties": {
          "sources": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "List of source file paths to copy."
          },
          "destination": {
            "type": "string",
            "description": "Destination directory path to copy the files to."
          }
        },
        "required": ["sources", "destination"]
      }
    }
  },
   {
    "type": "function",
    "function": {
      "name": "searchWeb",
      "description": "Searches the web using DuckDuckGo and returns the top k results with titles and links. Use this to find information online.",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "The search query to look up on the web."
          },
          "k": {
            "type": "integer",
            "description": "Number of results to return (default is 3).",
            "default": 3
          }
        },
        "required": ["query"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "extractTextFromUrl",
      "description": "Extracts the main text content from a webpage URL using a headless browser. Use this after searchWeb to read the actual content of a page.",
      "parameters": {
        "type": "object",
        "properties": {
          "url": {
            "type": "string",
            "description": "The URL of the webpage to extract text from."
          }
        },
        "required": ["url"]
      }
    }
  }

]
